import 'package:flutter/material.dart';

class NargesPage extends StatelessWidget {
  const NargesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
